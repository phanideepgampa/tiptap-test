# Changesets

This repo uses Changesets to document and version multi-package changes.

Quick commands:
- `pnpm changeset` — create a changeset
- `pnpm changeset version` — apply version bumps
- `pnpm -r build` — rebuild all packages

